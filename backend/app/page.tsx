export default function Page() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: '2rem' }}>
      <h1>Barber Backend API</h1>
      <p>This app directory hosts the backend API routes for the barber service.</p>
    </main>
  );
}
